import 'package:flutter/material.dart';

class Shirt2Screen extends StatelessWidget {
  const Shirt2Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.red,
        title: Text('Shirt2Screen'),
      ),
    );
  }
}
