import 'package:flutter/material.dart';

class Shirt1Screen extends StatelessWidget {
  const Shirt1Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.red,
        title: Text('Shirt1Screen'),
      ),
    );
  }
}
